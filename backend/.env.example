# =============================================================================
# Backend-Specific Environment Overrides
# =============================================================================
# This file documents backend-specific environment variables.
# Most configuration is in the root .env file.
# Only add variables here if you need to override root values for local development.

# Example: Override backend port for local development
# PORT=3001

# Example: Use different database for backend testing
# POSTGRES_DB=peloton_test_db
